# Cloudflare Workers AI credentials for the command line version.
# Copy this file to .env and fill it in, or just export these in your shell.
#
# Account ID is visible in your Cloudflare dashboard URL.
# The token needs BOTH "Workers AI - Read" and "Workers AI - Edit".
CF_ACCOUNT_ID=
CF_API_TOKEN=
